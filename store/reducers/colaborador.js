import { Tareas } from '../../data/datosEstaticos';

const estadoInicial = {
    tareas: Tareas,
};
const colaboradorReducer = (state = estadoInicial, action) => {
    return state;
}

export default colaboradorReducer;